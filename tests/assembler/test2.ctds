program {
	integer assert_equals(integer obtained, integer expected) extern;

    void main() {
        integer a = 17;

        integer b = 5;

        integer c = a / b;

        integer d = a % b;

        assert_equals(c + d, 5);
    }
}