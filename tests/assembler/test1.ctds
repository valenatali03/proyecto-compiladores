program {
    integer x = 5;

    integer print_int(integer x) extern;

    integer fact(integer x)
    {
        if (x < 2) then
        {
            return x;
        }
        
        return x * fact(x - 1);
    }

    void main() {
        print_int(fact(x));
    }
}