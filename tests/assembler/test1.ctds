program {
    integer x = 5;

    integer assert_equals(integer obtained, integer expected) extern;

    integer fact(integer x)
    {
        if (x < 2) then
        {
            return x;
        }
        
        return x * fact(x - 1);
    }

    void main() {
        assert_equals(fact(x), 120);
    }
}