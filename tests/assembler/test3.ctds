program {
	integer x = 5;

	integer assert_equals(integer obtained, integer expected) extern;

	integer inc(integer x)
    {
        return x + 1;
    }

	integer f(integer x)
    {
    	return x;
    }

    void main() {
        assert_equals(f(inc(x) + 1), 7);
    }
}