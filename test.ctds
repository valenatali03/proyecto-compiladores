program 
    {
		integer foo(integer x, integer y) {
			return x + y + x;
		}


		integer main() {
			integer y = 0;
			integer x = 0;
			x = (x+y) + (y+x);

			return (foo((x + 2) + (x + y), x + 3) + 2) + (x + y);
		}
    }