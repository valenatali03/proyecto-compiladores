program 
    {
		integer print_int(integer x, integer y) extern;

		integer inc(integer x) {
			return x + 1;
		}

		integer main() {
			integer x = 0;
			print_int(inc(x), inc(x));
			return x;
		}
    }